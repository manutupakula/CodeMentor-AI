# CodeMentor AI - Backend

See root [README.md](../README.md) for full system overview, API documentation, and setup instructions.

### Run Backend Server:
```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### Run Tests:
```bash
python -m pytest -v
```
